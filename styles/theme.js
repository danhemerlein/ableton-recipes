import generalScheme from './general';

const theme = {
  light: {
    background: '#FFFFFF',
    foreground: '#000000',
    anchor: '#000000',
    border: '#000000',
    general: generalScheme,
  },
};

export default theme;
